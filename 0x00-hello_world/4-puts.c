#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
	const char *msg = "Programming is like building a multilingual puzzle\n";
	puts(msg);
	return (0);
}
