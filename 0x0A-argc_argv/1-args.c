#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of arguments passed.
 * @argv: array of strings.
 * Return: 0 Success.
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
