#include <stdio.h>
/**
 * main - entry point
 * @argc: number of arguments passed
 * @argv: array of strings
 * Return: (0) Success
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
