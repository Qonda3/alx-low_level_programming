#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - prints last digit of a number
 *
 * Return: last digit
 */
int print_last_digit(int last)
{
	int res;

	res = last % 10;
	return (res);
}
