#include "main.h"

/**
 * main - check code
 *
 * Return: Always 0.
 */
int main(void)
{
	int i = 0;
	char word[] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r', '\n'};
	for (i = 0; i <= 8; i++)
	{
		_putchar(word[i]);
	}
	return (0);
}
